import 'package:flutter/material.dart';

class Mymap extends StatelessWidget {
  const Mymap({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: Text("HEY WORKERS"),
      ),
    );
  }
}